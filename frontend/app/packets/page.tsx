export default function PacketsPage() {
  return (
    <div className="page">
      <section className="hero">
        <span className="eyebrow">Packet Generation Placeholder</span>
        <h1>Packets</h1>
        <p className="hero-copy">
          Later stages will create per-job application packets with a tailored resume,
          cover letter, recruiter message, draft answers, notes, and change summary.
        </p>
      </section>

      <section className="panel">
        <h2>Stage 1 Goal</h2>
        <p className="subtle">
          The filesystem structure, database table, backend services, and output directory are
          now ready so packet generation can be added without restructuring the project.
        </p>
      </section>
    </div>
  );
}

