from __future__ import annotations


def discover_web_search_jobs(*_: object, **__: object) -> list[dict]:
    raise ValueError("Web search API is not configured. Add a provider later or use source URLs/company pages.")

